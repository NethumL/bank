<form method="post" action="{{ path('app_branch_delete', {'id': branch['ID']}) }}" onsubmit="return confirm('Are you sure you want to delete this branch?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ branch['ID']) }}">
    <button class="btn btn-danger">Delete</button>
</form>
