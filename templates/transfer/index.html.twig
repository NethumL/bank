{% extends 'base.html.twig' %}

{% block title %}Transfer Funds{% endblock %}

{% block body %}
<div class="row justify-content-md-center">
    <h2>Transfer Funds</h2>
    <div class="card w-50">
        <div class="card-body">
            {{ form_start(form) }}
            {{ form_errors(form) }}
            <div class="row mb-3">
                {{ form_label(form.from, 'From Account', {'label_attr': {'class': 'col-sm-4 col-form-label'}}) }}
                <div class="col-sm-8">
                    {{ form_widget(form.from) }}
                    {{ form_errors(form.from) }}
                </div>
            </div>
            <div class="row mb-3">
                {{ form_label(form.to, 'Beneficiary Account Number', {'label_attr': {'class': 'col-sm-4 col-form-label'}}) }}
                <div class="col-sm-8">
                    {{ form_widget(form.to) }}
                    {{ form_errors(form.to) }}
                </div>
            </div>
            <div class="row mb-3">
                {{ form_label(form.amount, null, {'label_attr': {'class': 'col-sm-4 col-form-label'}}) }}
                <div class="col-sm-8">
                    {{ form_widget(form.amount) }}
                    {{ form_errors(form.amount) }}
                </div>
            </div>
            <div class="row mb-3">
                {{ form_label(form.description, null, {'label_attr': {'class': 'col-sm-4 col-form-label'}}) }}
                <div class="col-sm-8">
                    {{ form_widget(form.description) }}
                    {{ form_errors(form.description) }}
                </div>
            </div>
            <div class="mt-3">
                {{ form_widget(form.transfer, {'attr': {'class': 'btn-success'}}) }}
            </div>
            {{ form_end(form) }}
        </div>
    </div>
</div>
{% endblock %}
